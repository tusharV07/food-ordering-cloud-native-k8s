spring:
  application:
    name: FoodCatalogue
  datasource:
    hikari:
      pool-name: FoodCatalogueHikariCP
      minimum-idle: 3
      maximum-pool-size: 10
      connection-timeout: 10000
      idle-timeout: 600000
      max-lifetime: 1700000
    #url: jdbc:mysql://localhost:3306/foodcataloguedb
    url: jdbc:mysql://host.docker.internal:3306/foodcataloguedb
    username: root
    password: ${DB_PWD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    
server:
  port: 9092

management:
  endpoints:
    web:
      exposure:
        include: "health,info,metrics"

eureka:
  client:
    service-url:
      #defaultZone: http://localhost:8761/eureka/
      defaultZone: http://eurekafd:8761/eureka/
    fetch-registry: true
    register-with-eureka: true