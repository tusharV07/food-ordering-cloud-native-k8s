spring:
  application:
    name: FoodCatalogue
  profiles:
    active: dev
  datasource:
    hikari:
      pool-name: FoodCatalogueHikariCP
      minimum-idle: 3
      maximum-pool-size: 10
      connection-timeout: 10000
      idle-timeout: 600000
      max-lifetime: 1700000
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    
server:
  port: 9092

management:
  endpoints:
    web:
      exposure:
        include: "health,info,metrics"
